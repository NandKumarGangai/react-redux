import {
    REQUEST_ITEMS,
    RECEIVE_ITEMS,
    SELECT_VIDEO,
    NEXT_PAGE_VIDEOS
  } from "../constants/consts";
import { items, videoToPlay, requestStatus } from './reducer';

describe('src\reducers\reducer.js', () => {
  it('Test', () => {
	expect('a').toEqual('a');
  });
});
